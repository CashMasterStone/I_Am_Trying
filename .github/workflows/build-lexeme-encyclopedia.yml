# BAABEL - Lexeme Encyclopedia Builder Workflow
# Author: Cyrus Makai Schoonover
# Repository: https://github.com/CashMasterStone/I_Am_Trying
# License: See LICENSE.md in repository root

name: BAABEL

on:
  push:
    branches:
      - Zenith
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: "3.10"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          if [ -f requirements.txt ]; then pip install -r requirements.txt; fi

      - name: Run Lexeme build
        run: |
          python3 scripts/build_lexeme_encyclopedia.py --input . --output build

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: lexeme-encyclopedia
          path: build/*
